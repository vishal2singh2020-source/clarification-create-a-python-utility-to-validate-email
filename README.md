# Email validation POC

Clarification: Create a Python utility to validate email addresses

Clarification requested for Jira issue KAN-1:

i had asked you to also add  changes like user should be able to enter the emails fro

## Local development

```bash
python -m unittest discover -s tests -p "test_*.py" -v
```

Extend `email_validate.py` with a `if __name__ == "__main__":` block when you want CLI demos.
